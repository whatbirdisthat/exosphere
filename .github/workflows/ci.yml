name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Build
        # Must precede the test step: the story suite spawns the built CLI (dist/bin.js),
        # which does not exist until the project is built.
        run: npm run build
      - name: Unit + integration + coverage (100% line+branch floor)
        run: npm run test:cov
      - name: Story / corpus gate
        run: npx vitest run tests/story
