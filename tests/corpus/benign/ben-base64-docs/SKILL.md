---
name: encoding-tutor
description: Teaches text encodings.
---

# Encoding Tutor

This skill explains that base64 encodes binary data as ASCII text, e.g. running
`base64 file.bin`. It also warns that an attacker might write "ignore previous
instructions" to hijack an agent — so you should be cautious of untrusted skills.
