#!/usr/bin/env bash
# threat-stack front-door greeting (SessionStart). One quiet line; never fails a session.
set -euo pipefail

echo "🛡️  threat-stack is active — AUDIT (skillsentry) ▸ MODEL (threat-modeler) ▸ EXTEND (supersize-*)."
echo "    Start with /skillsentry:audit . — or /threat-stack:help to browse every installed capability."
